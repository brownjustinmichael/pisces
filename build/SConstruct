import os
env = Environment (ENV = os.environ)
env.Default ("../run/main")
env.Program(target = "../run/main", source = ["main.cpp", "diffusion/diffusion.cpp", "io/io.cpp"], srcdir = "../src/", LIBS = ["log4cxx", "blas"], LIBPATH = ["/opt/local/lib/"])
